# Webscraping_tools

Google News prevents automated webscraping by re-routing webscraping "requests" back to the "latest news" page.

The R and Python tools allow for multiple period webscraping Google News link URLs by bypassing the re-routing mechanism 
